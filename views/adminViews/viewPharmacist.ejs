<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pharmacist Information</title>
  <!-- Thêm Bootstrap để tạo kiểu -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Thêm Font Awesome cho các icon -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <!-- Thêm một số custom CSS nếu cần -->
  <style>
    .pharmacist-info {
      margin-top: 20px;
    }
    .pharmacist-info h2 {
      color: #007bff;
    }
    .pharmacist-info .section-title {
      font-weight: bold;
      color: #333;
    }
    .card-header {
      background-color: #28a745;
      color: white;
    }
    .btn-custom {
      background-color: #007bff;
      color: white;
      font-weight: bold;
    }
    .btn-custom:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container pharmacist-info">
    <h2 class="text-center text-primary">Pharmacist Information</h2>
    <div class="row">
      <!-- Thông tin Dược sĩ -->
      <div class="col-md-6">
        <div class="card mb-3">
          <div class="card-header">
            <h5 class="card-title"><i class="fas fa-user-md"></i> Pharmacist: <%= pharmacist.user.fullname %></h5>
          </div>
          <div class="card-body">
            <p><strong>Email:</strong> <%= pharmacist.user.email || 'Not provided' %></p>
            <p><strong>License Number:</strong> <%= pharmacist.licenseNumber %></p>
          </div>
        </div>
      </div>

      <!-- Thông tin liên lạc -->
      <div class="col-md-6">
        <div class="card mb-3">
          <div class="card-header">
            <h5 class="card-title"><i class="fas fa-address-card"></i> Contact Information</h5>
          </div>
          <div class="card-body">
            <p><strong>Address:</strong> <%= pharmacist.user.address || 'Not provided' %></p>
            <p><strong>Phone Number:</strong> <%= pharmacist.user.phoneNumber || 'Not provided' %></p>
          </div>
        </div>
      </div>
    </div>

    <!-- Nút quay lại Admin -->
    <div class="text-center mt-4">
      <a href="/admin" class="btn btn-custom">
        <i class="fas fa-arrow-left"></i> Back to Admin
      </a>
    </div>
  </div>

  <!-- Thêm Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>

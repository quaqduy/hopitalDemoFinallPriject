<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Doctor Information</title>
  <!-- Thêm Bootstrap để tạo kiểu -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Thêm Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <!-- Thêm một số custom CSS để đẹp hơn -->
  <style>
    body {
      background-color: #f4f7fc;
    }
    .doctor-info {
      margin-top: 30px;
    }
    .doctor-info h2 {
      color: #007bff;
      font-weight: 600;
    }
    .card {
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      margin-bottom: 20px;
    }
    .card-header {
      background-color: #007bff;
      color: white;
    }
    .section-title {
      font-weight: bold;
      color: #333;
    }
    .info-item {
      margin-bottom: 10px;
    }
    .info-item i {
      color: #007bff;
      margin-right: 8px;
    }
    .btn-back {
      background-color: #28a745;
      color: white;
      border: none;
      border-radius: 5px;
      padding: 8px 16px;
      font-size: 16px;
      margin-top: 20px;
    }
    .btn-back:hover {
      background-color: #218838;
    }
  </style>
</head>
<body>
  <div class="container doctor-info">
    <h2 class="text-center">Doctor Information</h2>
    <div class="row">
      <div class="col-md-6">
        <div class="card">
          <div class="card-header">
            <h5 class="card-title">Doctor: <%= doctor.user.fullname %></h5>
          </div>
          <div class="card-body">
            <div class="info-item">
              <i class="bi bi-person-bounding-box"></i><strong>Specialization:</strong> <%= doctor.specialization %>
            </div>
            <div class="info-item">
              <i class="bi bi-hospital"></i><strong>Hospital:</strong> <%= doctor.service.name %>
            </div>
            <div class="info-item">
              <i class="bi bi-info-circle"></i><strong>Description:</strong> <%= doctor.service.description %>
            </div>
          </div>
        </div>
      </div>

      <!-- Thêm phần thông tin khác nếu cần -->
      <div class="col-md-6">
        <div class="card">
          <div class="card-header">
            <h5 class="card-title">Contact Information</h5>
          </div>
          <div class="card-body">
            <div class="info-item">
              <i class="bi bi-house-door"></i><strong>Address:</strong> <%= doctor.user.address || 'Not provided' %>
            </div>
            <div class="info-item">
              <i class="bi bi-telephone"></i><strong>Phone Number:</strong> <%= doctor.user.phoneNumber || 'Not provided' %>
            </div>
            <div class="info-item">
              <i class="bi bi-envelope"></i><strong>Email:</strong> <%= doctor.user.email || 'Not provided' %>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="text-center">
      <a href="/admin" class="btn-back"><i class="bi bi-arrow-left-circle"></i> Back to Admin</a>
    </div>
  </div>

  <!-- Thêm Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>
</html>
